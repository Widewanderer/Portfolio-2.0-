import { useLocation, useNavigate } from "react-router-dom";

export default function ProfilePic() {
    <div> 
        <img src = {require("../assets/HikingPic.jpg").default} alt = "Profile Picture" />
    </div>
}